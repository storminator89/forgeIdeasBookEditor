{
  "name": "bucherstellung",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "type": "module",
  "scripts": {
    "dev": "pnpm -r dev",
    "build": "pnpm -r build",
    "check-types": "pnpm -r check-types",
    "dev:native": "pnpm --filter native dev",
    "dev:web": "pnpm --filter web dev",
    "db:push": "pnpm --filter @bucherstellung/db db:push",
    "db:studio": "pnpm --filter @bucherstellung/db db:studio",
    "db:generate": "pnpm --filter @bucherstellung/db db:generate",
    "db:migrate": "pnpm --filter @bucherstellung/db db:migrate",
    "db:local": "pnpm --filter @bucherstellung/db db:local"
  },
  "dependencies": {
    "@bucherstellung/env": "workspace:*",
    "dotenv": "catalog:",
    "zod": "catalog:"
  },
  "devDependencies": {
    "@bucherstellung/config": "workspace:*",
    "@types/node": "^22.13.14",
    "typescript": "catalog:"
  },
  "packageManager": "pnpm@10.28.0"
}
